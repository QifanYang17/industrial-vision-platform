# .env.example — 复制为 .env 后按需修改（.env 不进 git）。
# pydantic-settings 会自动读取这些变量，作为配置的环境层覆盖。

# MLflow 追踪后端：默认 sqlite（file store 已于 2026-02 弃用）；接远程改成 http://host:5000
MLFLOW_TRACKING_URI=sqlite:///outputs/mlflow.db

# 默认设备：mps（M4 GPU）/ cpu / cuda
IVP_DEVICE=mps
